// Shared by the offscreen embedder and the build script. Keep in sync with
// scripts/build-embeddings.mjs — the runtime must embed queries with the same
// model that produced the corpus vectors.
export const MODEL_ID = 'FremyCompany/BioLORD-2023';
